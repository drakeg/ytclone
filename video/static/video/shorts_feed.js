(()=>{const feed=document.getElementById('shorts-feed');if(!feed)return;const items=[...feed.querySelectorAll('.shorts-item')],videos=items.map(i=>i.querySelector('.shorts-video')),playButtons=[...feed.querySelectorAll('[data-short-play]')],muteButtons=[...feed.querySelectorAll('[data-short-mute]')],commentForms=[...feed.querySelectorAll('[data-short-comment-form]')],reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let activeIndex=0,soundEnabled=false;const syncPlay=i=>{const v=videos[i],b=playButtons[i];if(!v||!b)return;const paused=v.paused;b.textContent=paused?'Play':'Pause';b.classList.toggle('is-paused',paused)};const syncSound=()=>{videos.forEach(v=>{v.muted=!soundEnabled});muteButtons.forEach((b,i)=>{b.textContent=soundEnabled?'Mute':'Unmute';b.setAttribute('aria-label',(soundEnabled?'Mute ':'Unmute ')+videos[i].getAttribute('aria-label'));b.setAttribute('aria-pressed',soundEnabled?'true':'false')})};const activate=i=>{activeIndex=Math.max(0,Math.min(i,items.length-1));videos.forEach((v,n)=>{v.muted=!soundEnabled;if(n!==activeIndex)v.pause()});videos[activeIndex]?.play().catch(()=>{});videos.forEach((_,n)=>syncPlay(n))};const togglePlay=i=>{const v=videos[i];if(!v)return;if(v.paused)v.play().catch(()=>{});else v.pause();syncPlay(i)};const move=i=>{const n=Math.max(0,Math.min(i,items.length-1));items[n].scrollIntoView({block:'start',behavior:reduced?'auto':'smooth'});activate(n)};const renderComment=(form,data)=>{const section=form.closest('.shorts-comments'),empty=section?.querySelector('[data-short-no-comments]'),count=section?.querySelector('[data-short-comment-count]');if(!section)return;if(empty)empty.remove();if(count)count.textContent=data.comment_count;const comment=document.createElement('div');comment.className='shorts-comment';comment.dataset.shortCommentId=String(data.id);const author=document.createElement('strong');author.textContent='@'+data.author;const when=document.createElement('span');when.className='video-meta';when.textContent=' just now';const body=document.createElement('div');body.textContent=data.comment;comment.append(author,when,body);const details=document.createElement('details');details.className='mt-2';const summary=document.createElement('summary');summary.className='small';summary.textContent='Reply';const replyForm=document.createElement('form');replyForm.className='shorts-reply-form mt-2';replyForm.method='post';replyForm.action=data.reply_url;const csrf=form.querySelector('input[name="csrfmiddlewaretoken"]')?.cloneNode(true);const textarea=document.createElement('textarea');textarea.className='form-control form-control-sm mb-2';textarea.name='comment';textarea.required=true;textarea.placeholder='Reply to @'+data.author+'…';textarea.setAttribute('aria-label','Reply to '+data.author);const button=document.createElement('button');button.className='btn btn-sm btn-outline-primary';button.type='submit';button.textContent='Post reply';if(csrf)replyForm.append(csrf);replyForm.append(textarea,button);details.append(summary,replyForm);comment.append(details);form.parentNode.insertBefore(comment,form)};const submitComment=async form=>{const error=form.querySelector('[data-short-comment-error]'),button=form.querySelector('button[type="submit"]'),textarea=form.querySelector('textarea[name="comment"]');if(error)error.style.display='none';if(button)button.disabled=true;try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{'X-Requested-With':'XMLHttpRequest','Accept':'application/json'}});const data=await response.json();if(!response.ok)throw new Error(data?.errors?'validation':'comment request failed');renderComment(form,data);form.reset();textarea?.focus()}catch(_){if(error)error.style.display='inline'}finally{if(button)button.disabled=false}};const observer=new IntersectionObserver(es=>{let b=null;es.forEach(e=>{if(e.isIntersecting&&(!b||e.intersectionRatio>b.intersectionRatio))b=e});if(b&&b.intersectionRatio>=.6)activate(Number(b.target.dataset.shortIndex))},{root:feed,threshold:[.6,.75,.9]});items.forEach(i=>observer.observe(i));feed.addEventListener('keydown',e=>{if(e.target.closest('button,a,input,textarea,select,summary'))return;if(e.key==='ArrowDown'||e.key==='PageDown'){e.preventDefault();move(activeIndex+1)}else if(e.key==='ArrowUp'||e.key==='PageUp'){e.preventDefault();move(activeIndex-1)}else if(e.key===' '||e.code==='Space'){e.preventDefault();togglePlay(activeIndex)}});videos.forEach((v,i)=>{v.addEventListener('click',()=>togglePlay(i));v.addEventListener('play',()=>syncPlay(i));v.addEventListener('pause',()=>syncPlay(i))});playButtons.forEach((b,i)=>b.addEventListener('click',()=>togglePlay(i)));muteButtons.forEach(b=>b.addEventListener('click',()=>{soundEnabled=!soundEnabled;syncSound();if(!videos[activeIndex]?.paused)videos[activeIndex]?.play().catch(()=>{})}));commentForms.forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();submitComment(form)}));document.addEventListener('visibilitychange',()=>document.hidden?videos.forEach(v=>v&&v.pause()):activate(activeIndex));syncSound();videos.forEach((_,i)=>syncPlay(i));activate(0)})();
